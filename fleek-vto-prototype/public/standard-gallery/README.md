# Standard Gallery Images

Drop your real JPG assets here to replace the default placeholders used by the standard gallery.

Expected filenames:

- `front.jpg` — front view image
- `side-chart.jpg` — size chart image
- `video.jpg` — video thumbnail image

Once you add them, the product details page will load them automatically.
